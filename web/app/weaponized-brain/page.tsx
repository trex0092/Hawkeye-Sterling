// Legacy entry — /weaponized-brain redirects to /workbench (Manifest tab)
// via next.config.mjs. This file remains as a fallback so direct deep-links
// still resolve while the redirect propagates through edge caches.
export { default } from "@/components/brain/BrainComponents";
