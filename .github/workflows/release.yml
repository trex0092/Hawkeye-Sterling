name: Release

# Diagnostic: absolute minimum workflow to verify GitHub can create jobs.
# If this still produces 0 jobs and instant failure, the issue is at
# GitHub's repo configuration level (required workflow check), not the YAML.

on:
  push:
  pull_request:

jobs:
  pass:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release workflow — pass."
