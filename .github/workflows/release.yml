name: Release

# Step 2 diagnostic: on:push + pull_request:branches:[main], single always-pass job.

on:
  push:
  pull_request:
    branches: [main]

jobs:
  pass:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release workflow — pass."
